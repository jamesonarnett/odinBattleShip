import styles from "../scss/TextBox.module.scss";

const TextBox = () => {
  return (
    <div className={styles.textBox}>
      <p>action reports here...</p>
    </div>
  );
};

export default TextBox;
